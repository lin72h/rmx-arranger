# bl-000 — Backlog overview + index

Work hierarchy: **`roadmap → backlog → op-NNN`** (most abstract → in-flight; see
[terminology.md](../terminology.md) §6). The [roadmap](../roadmap.md) names *what usable 1.0
means and the gate ladder A–F*; each roadmap gate spawns backlog items here; a backlog item is
*fetched* into op(s).

The **backlog** is the instruction stream *upstream of issue* — items identified but **not
yet fetched into the IDQ/ROB**. In the CPU/OoO model the rest of the workflow uses:

- **backlog (`bl-NNN`)** = instructions sitting in memory, not yet fetched. Planning-level.
  No pipeline, no agent assigned, no evidence spend.
- **op (`op-NNN`)** = an instruction that has been *fetched → decoded → issued*; now in
  flight in the IDQ/ROB (executing / waiting / retiring), owned by a pipeline + agent.

**Promotion = fetch.** When a `bl-NNN`'s preconditions are met and the Coordinator decides
to work it, it is *fetched*: decomposed into one or more `op-NNN` and issued. The backlog
item then points at the op(s) that carry it and is no longer planning-level.

`bl-NNN` is zero-padded, sequential, never reused (same discipline as `op-NNN`). `bl-000`
is this index; entries start at `bl-001`.

## State vocabulary (mirrors the op states)

| state | meaning (backlog) |
|---|---|
| **WAITING** | preconditions unmet — names what it waits on: a Coordinator decision, or an in-flight `op-NNN` that must retire first. Analogous to an op in the reservation station. |
| **READY** | preconditions met; fetchable now — ready to be decomposed into op(s) and issued. |
| **FETCHED → op-NNN** | promoted out of the backlog into flight; tracked thereafter by its op id(s) (the ROB owns it). Kept here with the pointer. |
| **RETIRED** | its op-chain completed. Terminal; kept for history. |
| **DROPPED** | abandoned before promotion (decision reversed / superseded). Kept for history with the reason. |

## Index

| id | title | state | blocked-on / pointer |
|---|---|---|---|
| [bl-001](bl-001-revive-kernel-kevent64.md) | Revive kernel `kevent64` (NextBSD #370), retire userland shim | **WAITING** | Coordinator decisions (strategy gate A-vs-B; placement). Recommended fetch *after* op-091 closes. |
| [bl-002](bl-002-qos-attr-darwin-parity-unspecified.md) | QoS-attr Darwin-parity: reject `QOS_CLASS_UNSPECIFIED` on set | **WAITING** | Coordinator decision (match Darwin EINVAL vs keep accept-and-normalize). Low priority; cataloged from op-090V. |
| [bl-003](bl-003-kernel-evfilt-machport-filter.md) | Kernel `EVFILT_MACHPORT` filter (`filt_machport`) — wire the Mach-port kqueue filter | **DROPPED** (op-098) | Premise false: `mach.ko` registers a real `filt_machport` (ipc_pset.c:504 / mach_module.c:270); op-091 grep was `sys/kern/`-scoped. op-098 DTrace confirmed standard kqueue path end-to-end. |
| [bl-004](bl-004-non-normal-qos-timer-fflags.md) | non-NORMAL QoS dispatch timers — XNU `NOTE_CRITICAL`/`NOTE_BACKGROUND` fflags vs `filt_timervalidate` | **WAITING** | Coordinator decision (userland-normalize vs kernel-side accept). Latent; NORMAL-QoS `dispatch_after` unaffected (closed at 129ee3c). |
| [bl-005](bl-005-semaphore-timedwait-polling.md) | `_dispatch_posix_sem_timedwait` 1ms poll → proper absolute-deadline primitive | **WAITING** | Coordinator decision (`sem_clockwait` vs absolute `sem_timedwait`). Production-hardening; cataloged from op-095V reserve point. |
| [bl-006](bl-006-libdispatch-runtime-conformance-soak-harness.md) | libdispatch runtime conformance + soak harness (truly-green template for Gate A/B/D) | **RETIRED** (op-102 + op-108) | All 3 truly-green legs DONE (9-case core): matrix green+traced + conformance MATCH 9/9 (op-102); oracles-hold-over-2h-soak (op-108, inherited). `data`/`io` cataloged for pass-2. Template for bl-010/011. |
| [bl-007](bl-007-continuous-soak-harness-infra.md) | continuous-soak harness infra (long-bhyve + continuous DTrace) — Gate D prerequisite | **RETIRED** (op-104) | op-104 proof passed 2026-06-23 (451 iters/0 fails/120s/status=0; tick-120s fired, 4 deltas). op-105 wakes. |
| [bl-008](bl-008-fine-grained-qos-kernel-propagation.md) | fine-grained QoS: kernel-propagated QoS (kevent_qos/workloops/voucher QoS) | **WAITING (pending)** | Big-ticket gated on bl-001 + Coordinator A-vs-B. Cheap userland `rtprio_thread` QoS-ordering sub-item fetchable independently. |
| [bl-009](bl-009-kernel-uaf-pset-receive-filt-machport.md) | kernel UAF: `ipc_mqueue_pset_receive` via `filt_machport` (compat/mach) | **RETIRED** (op-107) | Fix `e101f9c` (temp-ref+revalidate) PROVEN both halves: correctness op-109 (GLM/DS4P 9/10) + under-load op-108 (2h/27293 iters/0 panic vs op-105 iter-1 GPF; no leak). op-105 RETIRED. |
| [bl-010](bl-010-libnotify-notifyd-conformance-bringup.md) | libnotify + notifyd: conformance bring-up (Gate B next rung, Gate C real service) | **FETCHED → op-110** | First op on the 1.0-preview critical path. op-110 = Explorer bring-up + matrix + conformance diff (legs 1-3); soak leg = separate future Gatekeeper op. |
| [bl-011](bl-011-libasl-syslogd-conformance-bringup.md) | libasl + syslogd/aslmanager: conformance bring-up (Gate B next rung, Gate C real service) | **WAITING** | Sibling of bl-010. Apple ASL import in tree (`lib/libasl` 29 files, `usr.sbin/asl/syslogd`); two-syslogd ownership to settle. No USDT → fbt + fs oracle. |
| [bl-012](bl-012-release-distribution-image-gate-f.md) | release / distribution image: bootable rmxOS USB + ISO (Gate F logistics) | **WAITING** | FreeBSD release machinery inherited (`release.sh`, `make-memstick.sh`, `mkisoimages.sh`, `bsdinstall`). Tier 0 dev-preview USB fetchable early; Tier 1 (1.0) gated on A–E + third-party reproducibility. |
| [bl-013](bl-013-chaos-testing-fault-injection.md) | chaos-testing: add fault-injection to the testing strategy (future) | **WAITING (future)** | Additive to proven soak-testing (not a rename — terminology §8). Forces race windows deterministically (vs soak's luck-of-timing). In-tree hooks: `KFAIL_POINT` (unused) + DTrace destructive actions. Post-1.0 hardening. |

## Maintenance

Add a row when a `bl-NNN` is raised; update its state as it moves WAITING → READY →
FETCHED → RETIRED. When fetched, record the `op-NNN` it became in both this row and the
entry file. Keep this index terse — one row per item; detail lives in the entry file.
